# Project documentation

Human- and agent-oriented notes live here—not under `node_modules/`.

Suggested files (create as needed):

- `tech-stack.md` — versions, key libraries
- `conventions.md` — style, folders, naming
- `nextjs.md` (or equivalent) — framework-specific rules aligned with **your** pinned major version

Ingest substantive content into the project RAG so all agents retrieve it consistently.
